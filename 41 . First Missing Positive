class Solution:
    def firstMissingPositive(self, nums: List[int]) -> int:
        num_s=set(nums)
        i=1
        while True:
            if i not in num_s:
                return i
            i+=1
