class Solution(object):
    def removeElement(self, nums, val):
        """
        :type nums: List[int]
        :type val: int
        :rtype: int
        """
        n=len(nums)
        c=0
        for i in range(n):
            if val!=nums[i]:
               nums[c]=nums[i]
               c+=1
        return c
